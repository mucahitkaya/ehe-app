const GOOGLE_API_KEY = "blabla";

function getMapPreview({ lat, lng }) {
  const imagePreviewUrl = `google mapin sana verdiği url center =${lat},${lng}`;
  return imagePreviewUrl;
}
// sonra da bunu bir image içerisinde yazacaz
